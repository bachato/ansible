---

- hosts: all
  become: true
  tasks:

  - name: update repository index
    apt:
      update_cache: yes

  - name: install lsd package
    apt:
      name: lsd
      state: latest

  - name: install tldr package
    apt:
      name: tldr
      state: latest
